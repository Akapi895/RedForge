name: "jaeles"
command: "jaeles"
args: ["scan"]
enabled: true
short_description: "Advanced vulnerability scanner with custom signature support"
description: |
  Jaeles is an advanced vulnerability scanner that supports custom signatures for vulnerability detection.

  **Main features:**
  - Custom signature support
  - Detection of multiple vulnerability types
  - Fast scanning
  - Detailed reports

  **Use cases:**
  - Vulnerability scanning
  - Web application security testing
  - Custom detection rules
  - Security testing
parameters:
  - name: "url"
    type: "string"
    description: "Target URL"
    required: true
    flag: "-u"
    format: "flag"
  - name: "signatures"
    type: "string"
    description: "Path to custom signatures"
    required: false
    flag: "-s"
    format: "flag"
  - name: "config"
    type: "string"
    description: "Configuration file"
    required: false
    flag: "-c"
    format: "flag"
  - name: "threads"
    type: "int"
    description: "Thread count"
    required: false
    flag: "-t"
    format: "flag"
    default: 20
  - name: "timeout"
    type: "int"
    description: "Request timeout (seconds)"
    required: false
    flag: "--timeout"
    format: "flag"
    default: 20
  - name: "additional_args"
    type: "string"
    description: |
      Additional Jaeles arguments for options not defined in the parameter list.

      **Example values:**
      - Add common argument examples based on the tool's capabilities

      **Notes:**
      - Separate multiple arguments with spaces
      - Ensure the argument format is correct to prevent command injection
      - These arguments are appended directly to the end of the command
    required: false
    format: "positional"
