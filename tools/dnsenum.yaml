name: "dnsenum"
command: "dnsenum"
enabled: true
short_description: "DNS enumeration tool"
description: |
  DNSenum is a DNS information-gathering tool for enumerating DNS information.

  **Main features:**
  - DNS information gathering
  - Subdomain enumeration
  - Zone-transfer testing
  - Reverse lookups

  **Use cases:**
  - DNS enumeration
  - Subdomain discovery
  - Network reconnaissance
  - Penetration testing
parameters:
  - name: "domain"
    type: "string"
    description: "Target domain"
    required: true
    position: 0
    format: "positional"
  - name: "dns_server"
    type: "string"
    description: "DNS server to use"
    required: false
    flag: "-n"
    format: "flag"
  - name: "wordlist"
    type: "string"
    description: "Wordlist file for brute forcing"
    required: false
    flag: "-f"
    format: "flag"
  - name: "additional_args"
    type: "string"
    description: |
      Additional dnsenum arguments. Use this to pass dnsenum options not defined in the parameter list.

      **Example values:**
      - Add common argument examples based on the tool's capabilities

      **Notes:**
      - Separate multiple arguments with spaces
      - Ensure the argument format is correct to prevent command injection
      - These arguments are appended directly to the end of the command
    required: false
    format: "positional"
