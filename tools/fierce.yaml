name: "fierce"
command: "fierce"
enabled: true
short_description: "DNS reconnaissance tool"
description: |
  Fierce is a DNS reconnaissance tool for discovering subdomains of target domains.

  **Main features:**
  - Subdomain discovery
  - DNS brute forcing
  - Zone-transfer testing
  - Network mapping

  **Use cases:**
  - DNS reconnaissance
  - Subdomain enumeration
  - Network mapping
  - Penetration testing
parameters:
  - name: "domain"
    type: "string"
    description: "Target domain"
    required: true
    flag: "-dns"
    format: "flag"
  - name: "dns_server"
    type: "string"
    description: "DNS server to use"
    required: false
    flag: "-dnsserver"
    format: "flag"
  - name: "additional_args"
    type: "string"
    description: |
      Additional fierce arguments. Use this to pass fierce options not defined in the parameter list.

      **Example values:**
      - Add commonly used arguments according to the tool's capabilities

      **Notes:**
      - Separate multiple arguments with spaces
      - Ensure the argument format is correct to avoid command injection
      - This parameter is appended directly to the end of the command
    required: false
    format: "positional"
